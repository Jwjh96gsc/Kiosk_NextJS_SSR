export interface Showtime {
  filmCode: string;
  image: string;
  name: string;
  hall: string;
  hallType: string;
  language: string;
  showDate: string; // Store as ISO string
  showTime: string; // Store as ISO string
  showDateTime: string; // Store as ISO string
  id: string;
}